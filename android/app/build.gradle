plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.link_up"
    compileSdk = 34 // Replace flutter.compileSdkVersion with the SDK version you're using
    ndkVersion = "25.1.8937393"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }

    defaultConfig {
        applicationId = "com.example.link_up"
        minSdk = 21 // Replace minSdkVersion with an explicit value
        targetSdk = 33 // Replace flutter.targetSdkVersion with the SDK version you're targeting
        versionCode = 1 // Replace flutter.versionCode with an explicit value
        versionName = "1.0.0" // Replace flutter.versionName with an explicit value
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

